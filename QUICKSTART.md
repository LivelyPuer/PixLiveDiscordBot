# 🚀 Быстрый старт развертывания

## Одна команда для полной установки на сервер

### Способ 1: Прямо с GitHub (рекомендуется)

```bash
curl -fsSL https://raw.githubusercontent.com/LivelyPuer/PixLiveDiscordBot/main/install.sh | sudo bash
```

### Способ 2: С локального репозитория

```bash
git clone https://github.com/LivelyPuer/PixLiveDiscordBot.git
cd PixLiveDiscordBot
sudo bash install.sh
```

## Что произойдет

Скрипт `install.sh` автоматически:

1. ✅ Определит вашу ОС (Ubuntu, CentOS, Alpine, etc.)
2. ✅ Установит Python, git и необходимые пакеты
3. ✅ Создаст пользователя `pixlive` для бота
4. ✅ Клонирует репозиторий в `/opt/PixLiveDiscordBot`
5. ✅ Создаст виртуальное окружение Python (venv)
6. ✅ Установит все зависимости из `requirements.txt`
7. ✅ **Настроит автозапуск через systemd** (главное!)
8. ✅ **Запустит бота**

## ⚙️ Конфигурация

После установки скрипт попросит отредактировать `.env`:

```bash
nano /opt/PixLiveDiscordBot/.env
```

Добавь необходимые токены (см. [SETUP.md](SETUP.md)):

```env
DISCORD_TOKEN=your_token_here
DISCORD_CHANNEL_ID=123456789
TG_BOT_TOKEN=your_token_here
TG_ADMIN_PASSWORD=your_password_here
DEVIANTART_CLIENT_ID=your_id_here
DEVIANTART_CLIENT_SECRET=your_secret_here
DEVIANTART_USERNAMES=artist1,artist2
```

## 🎮 Управление ботом

Все команды используют systemd:

```bash
# Запустить
sudo systemctl start pixlive

# Остановить
sudo systemctl stop pixlive

# Перезагрузить
sudo systemctl restart pixlive

# Проверить статус
sudo systemctl status pixlive

# Логи (нажми Ctrl+C для выхода)
sudo journalctl -u pixlive -f
```

## 🔄 Обновление

### Быстрое обновление одной командой:

```bash
sudo bash /opt/PixLiveDiscordBot/update.sh
```

### Или вручную:

```bash
cd /opt/PixLiveDiscordBot
sudo git pull origin main
sudo systemctl restart pixlive
```

## 📊 Что такое автозапуск?

После установки бот:

- 🔄 **Будет запускаться автоматически** при перезагрузке сервера
- 🔁 **Будет автоматически перезагружаться**, если упадет
- 📋 **Логировать свою работу** в systemd journal

Это означает, что вы можете:
- Перезагрузить сервер - бот запустится автоматически
- Не беспокоиться если бот упадет - он перезагрузится

## 📁 Файлы после установки

```
/opt/PixLiveDiscordBot/
├── venv/                  # Виртуальное окружение (не трогай!)
├── .env                   # Твоя конфигурация (ВАЖНО: храни в безопасности)
├── main.py               # Точка входа
├── requirements.txt      # Зависимости Python
├── install.sh            # Скрипт установки
├── update.sh             # Скрипт обновления
├── deploy.sh             # Полное управление
├── run.sh                # Локальный запуск
├── DEPLOYMENT.md         # Подробная документация
└── ... (остальные файлы)
```

## 🆘 Если что-то не работает

### Проверить логи

```bash
# Полные логи
sudo journalctl -u pixlive -f

# Последние 50 строк
sudo journalctl -u pixlive -n 50

# Только ошибки
sudo journalctl -u pixlive -p err
```

### Проверить конфигурацию

```bash
# Открыть .env
nano /opt/PixLiveDiscordBot/.env

# Запустить вручную чтобы увидеть ошибки
/opt/PixLiveDiscordBot/venv/bin/python /opt/PixLiveDiscordBot/main.py
```

### Перезапустить вручную

```bash
sudo systemctl restart pixlive
sleep 2
sudo systemctl status pixlive
```

## 📖 Подробная информация

- [DEPLOYMENT.md](DEPLOYMENT.md) - Полное руководство по развертыванию
- [SETUP.md](SETUP.md) - Инструкция по получению токенов
- [README.md](README.md) - Основная документация проекта

---

## 💡 Советы

1. **Сохраняй .env в безопасности** - там хранятся твои секретные токены
2. **Регулярно обновляй** - используй `sudo bash update.sh`
3. **Проверяй логи** - если что-то не так, первое что нужно сделать это посмотреть логи
4. **Документируй свои изменения** - если что-то поправил, запиши что и почему

---

**Готово! Бот установлен и работает. 🎉**
