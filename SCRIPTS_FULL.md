# 📋 Документация скриптов развертывания

## Обзор

Проект содержит несколько скриптов для упрощения развертывания, управления и обновления бота:

| Скрипт | Использование | Требует root | Описание |
|--------|--------------|-------------|---------|
| [`install.sh`](#installsh) | `sudo bash install.sh` | ✅ | **Одна команда для полной установки** |
| [`deploy.sh`](#deploysh) | `sudo bash deploy.sh` | ✅ | Интерактивное меню управления |
| [`update.sh`](#updatesh) | `sudo bash update.sh` | ✅ | Быстрое обновление кода |
| [`run.sh`](#runsh) | `bash run.sh` | ❌ | Локальный запуск (разработка) |

---

## `install.sh`

### 🎯 Назначение
**Полная автоматическая установка на сервер одной командой**

### 📖 Использование

```bash
# С GitHub
curl -fsSL https://raw.githubusercontent.com/LivelyPuer/PixLiveDiscordBot/main/install.sh | sudo bash

# Локально
sudo bash install.sh
```

### 📋 Что делает

1. **Проверка ОС** - определяет Ubuntu/Debian, CentOS/RHEL, Alpine
2. **Установка зависимостей** - Python 3, git, build tools
3. **Создание пользователя** - создает `pixlive` для бота
4. **Клонирование кода** - из GitHub в `/opt/PixLiveDiscordBot`
5. **Python окружение** - создает venv и устанавливает requirements
6. **Systemd сервис** - создает файл сервиса для автоуправления
7. **Автозапуск** - включает автоматический запуск при старте
8. **Запуск** - стартует бота

### ✅ Результат

- Бот установлен в `/opt/PixLiveDiscordBot`
- Работает под пользователем `pixlive`
- Управляется через `systemctl`
- **Автоматически запускается при перезагрузке сервера**
- **Автоматически перезагружается если упадет**

### 🎮 Команды после установки

```bash
sudo systemctl start pixlive      # Запустить
sudo systemctl stop pixlive       # Остановить
sudo systemctl restart pixlive    # Перезагрузить
sudo systemctl status pixlive     # Статус
sudo journalctl -u pixlive -f     # Логи
```

---

## `deploy.sh`

### 🎯 Назначение
**Полное управление ботом через интерактивное меню или команды**

### 📖 Использование

#### Интерактивное меню
```bash
sudo bash /opt/PixLiveDiscordBot/deploy.sh
```

Меню:
```
1) 📥 Полная установка (новая инсталляция)
2) 🔄 Обновить код и перезагрузить
3) ▶️  Запустить сервис
4) ⏹️  Остановить сервис
5) 🔁 Перезагрузить сервис
6) ℹ️  Показать статус
7) 📋 Показать логи
8) 🚪 Выход
```

#### Команды
```bash
sudo bash deploy.sh install       # Полная установка
sudo bash deploy.sh update        # Обновление
sudo bash deploy.sh start         # Запустить
sudo bash deploy.sh stop          # Остановить
sudo bash deploy.sh restart       # Перезагрузить
sudo bash deploy.sh status        # Статус
sudo bash deploy.sh logs          # Логи
```

### 📋 Функции

- ✅ Установка системных зависимостей
- ✅ Создание пользователя и папок
- ✅ Настройка Python окружения
- ✅ Создание systemd сервиса
- ✅ Управление сервисом
- ✅ Просмотр статуса и логов
- ✅ Красивый интерфейс с цветами

### 💡 Когда использовать

- Нужна полная переустановка
- Нужно повторно настроить сервис
- Не хочешь использовать `install.sh` (может быть уже установлено)

---

## `update.sh`

### 🎯 Назначение
**Быстрое обновление кода и перезагрузка сервиса**

### 📖 Использование

```bash
sudo bash /opt/PixLiveDiscordBot/update.sh
```

### 📋 Что делает

1. Обновляет код из GitHub (`git pull`)
2. Обновляет Python зависимости
3. Перезагружает systemd сервис
4. Проверяет что сервис запустился

### 🎯 Результат

Наиболее свежая версия кода запущена и работает

### 💡 Когда использовать

- После выпуска новой версии на GitHub
- Для обновления зависимостей
- Когда нужно быстро обновить и перезагрузить

### 📅 Автоматизация (опционально)

Можно добавить в cron для автоматического обновления каждый день:

```bash
sudo crontab -e

# Добавь строку:
0 2 * * * /opt/PixLiveDiscordBot/update.sh
```

---

## `run.sh`

### 🎯 Назначение
**Локальный запуск без root (для разработки и тестирования)**

### 📖 Использование

```bash
cd /Users/maksim/Yandex.Disk.localized/AIProjects/PixLiveDiscordBot
bash run.sh
```

### 📋 Что делает

1. Проверяет наличие `.env` (создает из примера если нужно)
2. Создает виртуальное окружение если не существует
3. Обновляет pip
4. Устанавливает зависимости
5. Запускает бота

### 🎯 Результат

Бот запущен локально (не как сервис, просто в консоли)

### 💡 Когда использовать

- Локальная разработка
- Тестирование перед развертыванием
- Быстрая проверка что все работает
- На машине разработчика

### ⌨️ Остановка

Нажми `Ctrl+C` для остановки

---

## 📊 Сравнение скриптов

### Кто создает что

| Компонент | `install.sh` | `deploy.sh` | `update.sh` | `run.sh` |
|-----------|-------------|-----------|-----------|---------|
| Зависимости ОС | ✅ | ✅ | ❌ | ❌ |
| Пользователь `pixlive` | ✅ | ✅ | ❌ | ❌ |
| Клон репо | ✅ | ✅ | ❌ | ❌ |
| venv | ✅ | ✅ | ❌ | ✅ |
| requirements | ✅ | ✅ | ✅ | ✅ |
| systemd сервис | ✅ | ✅ | ❌ | ❌ |
| Запуск сервиса | ✅ | ✅ | ✅ | ❌ |
| Требует root | ✅ | ✅ | ✅ | ❌ |

---

## 🔄 Типичные сценарии использования

### Сценарий 1: Новый сервер

```bash
# 1. Одна команда для всего
sudo bash install.sh

# 2. Отредактировать .env
nano /opt/PixLiveDiscordBot/.env

# 3. Готово! Бот работает
```

### Сценарий 2: Обновление кода

```bash
# Одна команда
sudo bash /opt/PixLiveDiscordBot/update.sh

# Или вручную
cd /opt/PixLiveDiscordBot
sudo git pull
sudo systemctl restart pixlive
```

### Сценарий 3: Локальная разработка

```bash
# В папке проекта
bash run.sh

# Редактируешь код, перезапускаешь Ctrl+C и `bash run.sh`
```

### Сценарий 4: Полная переустановка

```bash
# Если что-то сломалось
sudo bash /opt/PixLiveDiscordBot/deploy.sh install

# Или выбери пункт меню
sudo bash /opt/PixLiveDiscordBot/deploy.sh
```

### Сценарий 5: Проверка статуса

```bash
# Что-то не работает?
sudo systemctl status pixlive          # Статус
sudo journalctl -u pixlive -f          # Логи
sudo journalctl -u pixlive -n 100      # Последние 100 строк
```

---

## ⚙️ Конфигурация systemd сервиса

Скрипты создают systemd сервис с этими параметрами:

```ini
[Unit]
Description=PixLive Discord Bot
After=network.target

[Service]
Type=simple
User=pixlive
WorkingDirectory=/opt/PixLiveDiscordBot
ExecStart=/opt/PixLiveDiscordBot/venv/bin/python main.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal
SyslogIdentifier=pixlive
Environment="PYTHONUNBUFFERED=1"

[Install]
WantedBy=multi-user.target
```

Значит:
- **Type=simple** - обычный процесс
- **Restart=always** - всегда перезагружается если упадет
- **RestartSec=10** - ждет 10 секунд перед перезагрузкой
- **StandardOutput=journal** - логи в systemd journal
- **WantedBy=multi-user.target** - запускается при старте системы

---

## 📖 Дополнительная информация

- [DEPLOYMENT.md](DEPLOYMENT.md) - Полное руководство по развертыванию
- [QUICKSTART.md](QUICKSTART.md) - Быстрый старт
- [SETUP.md](SETUP.md) - Инструкция по токенам
- [README.md](README.md) - Основная документация

---

## 🆘 Решение проблем

### Скрипт требует sudo но я запустил его без sudo

Нужно запустить с `sudo`:
```bash
sudo bash install.sh      # ✅ Правильно
bash install.sh           # ❌ Неправильно
```

### Не могу редактировать .env

Может быть заблокирован. Проверь права:
```bash
ls -la /opt/PixLiveDiscordBot/.env
sudo chown pixlive:pixlive /opt/PixLiveDiscordBot/.env
```

### Сервис не запускается

1. Проверь логи: `sudo journalctl -u pixlive -f`
2. Проверь .env: `cat /opt/PixLiveDiscordBot/.env`
3. Проверь что сервис существует: `sudo systemctl status pixlive`

### Git ошибка при обновлении

Может быть проблема с правами доступа:
```bash
cd /opt/PixLiveDiscordBot
sudo chown -R pixlive:pixlive .git
sudo git pull
```

---

**Всегда смотри логи для деталей! 📋**
