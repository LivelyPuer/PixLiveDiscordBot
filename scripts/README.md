# 🚀 Скрипты PixLive Bot

Автоматизированные скрипты для управления, развёртывания и запуска Discord бота.

## 📋 Доступные скрипты

### 1️⃣ **setup.sh** — Полная установка (требует root)

**Использование:**
```bash
sudo bash scripts/setup.sh
```

**Что делает:**
- ✅ Проверяет root права
- ✅ Обнаруживает операционную систему (macOS, Linux)
- ✅ Устанавливает зависимости (Python, pip, git, systemd)
- ✅ Клонирует репозиторий (опционально)
- ✅ Создаёт пользователя `pixlive-bot` (Linux только)
- ✅ Создаёт systemd сервис для автозапуска
- ✅ Устанавливает требуемые Python пакеты
- ✅ Генерирует .env файл (интерактивно)
- ✅ Запускает бота в фоне

**Интерактивное меню:**
```
1. Установить зависимости и создать юзера
2. Установить/обновить зависимости Python
3. Сгенерировать файл .env (интерактивно)
4. Создать systemd сервис
5. Запустить бота (systemctl start pixlive-bot)
6. Просмотреть статус (systemctl status pixlive-bot)
0. Выход
```

---

### 2️⃣ **update.sh** — Обновление кода (не требует root)

**Использование:**
```bash
bash scripts/update.sh
```

**Что делает:**
- ✅ Определяет текущую ветку в git
- ✅ Стягивает последние изменения (`git pull`)
- ✅ Обновляет Python зависимости (`pip install -r requirements.txt`)
- ✅ Перезапускает systemd сервис (`systemctl restart pixlive-bot`)
- ✅ Показывает статус обновления

**Примечание:** Перезапуск сервиса требует прав через `sudo`, поэтому может попросить пароль.

**Опции:**
- `--test` или `-t` — Запустить тесты перед обновлением
- `--no-restart` — Не перезапускать сервис после обновления

**Примеры:**
```bash
# Обычное обновление
bash scripts/update.sh

# Обновление с тестами
bash scripts/update.sh --test

# Обновление без перезапуска
bash scripts/update.sh --no-restart
```

---

### 3️⃣ **run.sh** — Локальный запуск (разработка)

**Использование:**
```bash
bash scripts/run.sh
```

**Что делает:**
- ✅ Проверяет наличие виртуального окружения (venv)
- ✅ Создаёт venv если его нет (`python3 -m venv venv`)
- ✅ Активирует виртуальное окружение
- ✅ Проверяет requirements.txt
- ✅ Устанавливает недостающие пакеты
- ✅ Запускает конфигурационную проверку
- ✅ Запускает бота в интерактивном режиме

**Когда использовать:**
- 🔧 Локальная разработка
- 🧪 Тестирование изменений
- 📋 Проверка конфигурации перед продакшеном

---

### 4️⃣ **make_executable.sh** — Сделать скрипты исполняемыми

**Использование:**
```bash
bash scripts/make_executable.sh
```

**Что делает:**
- ✅ Добавляет права на исполнение для всех .sh файлов в scripts/
- ✅ Показывает список изменённых файлов

**После первого запуска можно использовать:**
```bash
# Вместо
bash scripts/setup.sh

# Можно просто
./scripts/setup.sh
```

---

## 🎯 Рекомендуемый порядок

### Для первой установки на сервер:

```bash
# 1. Клонировать репозиторий
git clone https://github.com/yourusername/PixLiveDiscordBot.git
cd PixLiveDiscordBot

# 2. Сделать скрипты исполняемыми
bash scripts/make_executable.sh

# 3. Запустить полную установку (требует root)
sudo ./scripts/setup.sh

# Следовать интерактивному меню в setup.sh
```

### Для локальной разработки:

```bash
# Запустить локально
bash scripts/run.sh

# Или если уже сделал make_executable.sh
./scripts/run.sh
```

### Для обновления кода на сервере:

```bash
# Обновить и перезапустить
bash scripts/update.sh

# С тестами
bash scripts/update.sh --test
```

---

## 🔧 Примеры использования

### Сценарий 1: Первая установка на Ubuntu сервер

```bash
ssh user@server
cd ~ && git clone https://github.com/yourusername/PixLiveDiscordBot.git
cd PixLiveDiscordBot
bash scripts/make_executable.sh
sudo ./scripts/setup.sh
# Выбрать пункты меню как требуется
```

### Сценарий 2: Локальное тестирование

```bash
# На своём компьютере
bash scripts/run.sh
# Бот запустится в интерактивном режиме
# Ctrl+C чтобы остановить
```

### Сценарий 3: Обновление на сервере

```bash
# Когда вышло обновление кода в репозитории
bash scripts/update.sh --test

# Проверить статус
systemctl status pixlive-bot
```

### Сценарий 4: Отладка systemd сервиса

```bash
# Просмотреть логи
journalctl -u pixlive-bot -f

# Или использовать setup.sh меню (пункт 6)
sudo bash scripts/setup.sh
# Выбрать пункт 6
```

---

## 🛡️ Безопасность

### 🔐 Требуемые права

| Скрипт | Root | Sudo | Зачем |
|--------|------|------|-------|
| setup.sh | ✅ Требуется | ✅ Используется | Установка системных пакетов, создание пользователя |
| update.sh | ❌ Нет | ✅ Требуется | Перезапуск systemd сервиса |
| run.sh | ❌ Нет | ❌ Не нужен | Локальный запуск в текущем окружении |
| make_executable.sh | ❌ Нет | ❌ Не нужен | Изменение прав на файлы в текущей папке |

### 🔒 Что скрипты НЕ делают

- ❌ Не требуют root для обновления кода
- ❌ Не изменяют системные конфигурации (кроме systemd)
- ❌ Не отправляют данные куда-либо
- ❌ Не требуют специальных разрешений для локального запуска

---

## 🆘 Troubleshooting

### "Permission denied" при запуске скрипта

```bash
# Сделать скрипт исполняемым
chmod +x scripts/setup.sh

# Или использовать
bash scripts/setup.sh
```

### "command not found: python3"

```bash
# Установить Python
# На macOS
brew install python3

# На Ubuntu/Debian
sudo apt-get install python3 python3-pip

# Затем снова запустить
bash scripts/run.sh
```

### "Ошибка: Not root user"

```bash
# setup.sh требует root права
# Используй sudo
sudo bash scripts/setup.sh

# update.sh не требует root для основного кода
bash scripts/update.sh
```

### systemd сервис не запускается

```bash
# Проверить статус
systemctl status pixlive-bot

# Просмотреть логи
journalctl -u pixlive-bot -n 50

# Или использовать интерактивное меню
sudo bash scripts/setup.sh
# Выбрать пункт 6
```

### "ModuleNotFoundError" при запуске бота

```bash
# Убедиться что requirements установлены
bash scripts/run.sh

# Или вручную
pip install -r requirements.txt
```

---

## 📊 Диагностика

### Проверить установку

```bash
# Все ли нужные пакеты установлены?
pip show discord.py python-telegram-bot aiohttp aiofiles

# Есть ли .env файл?
ls -la .env

# Запущен ли бот?
systemctl status pixlive-bot
```

### Просмотреть логи бота

```bash
# Последние 100 строк
journalctl -u pixlive-bot -n 100

# В реальном времени
journalctl -u pixlive-bot -f

# За последний час
journalctl -u pixlive-bot --since "1 hour ago"
```

---

## 📚 Связанные файлы

- [requirements.txt](../requirements.txt) — Зависимости Python
- [.env.example](../.env.example) — Пример конфигурации
- [SETUP.md](../SETUP.md) — Детальное руководство установки
- [DEPLOY.md](../DEPLOY.md) — Развёртывание на сервер
- [main.py](../main.py) — Точка входа приложения

---

## 🔄 Жизненный цикл

```
Первая установка
    ↓
sudo ./scripts/setup.sh
    ↓
Бот запущен в systemd
    ↓
Новое обновление кода?
    ↓
./scripts/update.sh
    ↓
Бот перезапущен автоматически
    ↓
Локальное тестирование?
    ↓
./scripts/run.sh
    ↓
Готово!
```

---

**Версия:** 1.0  
**Последнее обновление:** 16 декабря 2025  
**Требуемая версия Python:** 3.8+  
**Поддерживаемые ОС:** macOS, Linux (Ubuntu, Debian, Fedora)
