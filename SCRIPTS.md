# 🚀 Скрипты управления PixLive Bot

## 📋 Доступные скрипты

### 1️⃣ **setup.sh** — Полная установка на сервер

**Использование (требует root):**
```bash
sudo bash setup.sh
```

**Что делает:**
- ✅ Проверяет OS и зависимости
- ✅ Устанавливает Python, git, необходимые пакеты
- ✅ Создает пользователя `pixlive` для бота
- ✅ Клонирует/обновляет репозиторий
- ✅ Создает виртуальное окружение
- ✅ Устанавливает зависимости Python
- ✅ Создает systemd сервис для автозагрузки
- ✅ Запускает бота

**Интерактивное меню:**
```
1) Полная установка (новая инсталляция)
2) Обновить код и перезагрузить
3) Только обновить зависимости Python
4) Показать статус
5) Показать логи
6) Выход
```

**После установки бот будет:**
- 🔄 Автоматически запускаться при перезагрузке сервера
- 🔁 Автоматически перезагружаться если упадет
- 📊 Логировать в systemd (journalctl)

---

### 2️⃣ **update.sh** — Обновить код и перезагрузить

**Использование:**
```bash
bash update.sh
```

**Что делает:**
- ✅ Обновляет код из GitHub
- ✅ Обновляет зависимости Python
- ✅ Проверяет конфигурацию
- ✅ Запускает тесты (опционально)
- ✅ Перезагружает systemd сервис

**Опции:**
- Автоматически определяет текущую ветку git
- Предлагает запустить тесты перед перезагрузкой
- Не требует root прав (для перезагрузки может потребоваться)

---

### 3️⃣ **run.sh** — Быстрый запуск локально

**Использование:**
```bash
bash run.sh
```

**Что делает:**
- ✅ Проверяет .env (создает из примера если нужно)
- ✅ Создает виртуальное окружение если нужно
- ✅ Устанавливает зависимости если нужно
- ✅ Запускает бота

**Идеально для:**
- Локальной разработки
- Тестирования перед развертыванием
- Быстрого старта без сложной конфигурации

---

## 🎯 Быстрые примеры

### Первый раз на новом сервере

```bash
# 1. Клонировать репо
git clone https://github.com/LivelyPuer/PixLiveDiscordBot.git
cd PixLiveDiscordBot

# 2. Запустить установку
sudo bash setup.sh

# 3. Отредактировать .env если требуется
nano /opt/PixLiveDiscordBot/.env

# 4. Запустить сервис
sudo systemctl start pixlive

# 5. Смотреть логи
sudo journalctl -u pixlive -f
```

### Обновить код

```bash
cd /opt/PixLiveDiscordBot
bash update.sh
```

### Локальная разработка/тестирование

```bash
# Клонировать
git clone https://github.com/LivelyPuer/PixLiveDiscordBot.git
cd PixLiveDiscordBot

# Быстрый старт
bash run.sh
```

---

## 🔧 Systemd команды (после установки setup.sh)

```bash
# Статус сервиса
sudo systemctl status pixlive

# Запустить
sudo systemctl start pixlive

# Остановить
sudo systemctl stop pixlive

# Перезагрузить
sudo systemctl restart pixlive

# Показать логи (последние 50 строк)
sudo journalctl -u pixlive -n 50

# Логи в реальном времени
sudo journalctl -u pixlive -f

# Логи за последний час
sudo journalctl -u pixlive --since "1 hour ago"

# Логи с ошибками
sudo journalctl -u pixlive -p err
```

---

## 📊 Структура после setup.sh

```
/opt/PixLiveDiscordBot/          # Основная директория
├── main.py                       # Точка входа
├── .env                          # Конфигурация (защищена chmod 600)
├── venv/                         # Виртуальное окружение
├── data/
│   └── state.json                # Состояние (последние посты)
└── ...

/etc/systemd/system/pixlive.service  # Systemd конфиг

Пользователь: pixlive (создается автоматически)
Логи: journalctl (systemd)
```

---

## ⚙️ Параметры systemd сервиса

В `setup.sh` автоматически устанавливаются:

- **Restart=always** — перезагрузка при падении
- **RestartSec=10** — 10 сек между перезагрузками
- **MemoryMax=512M** — максимум памяти
- **CPUQuota=50%** — максимум CPU
- **StartLimitBurst=5** — макс 5 перезагрузок за
- **StartLimitInterval=600** — 10 минут

Отредактировать: `sudo nano /etc/systemd/system/pixlive.service`

---

## 🆘 Если что-то не работает

### setup.sh падает

```bash
# Запустить с debug информацией
bash -x setup.sh 2>&1 | tee setup.log

# Проверить логи
cat setup.log
```

### Бот не запускается после update.sh

```bash
# Проверить конфиг
python3 check_config.py

# Посмотреть логи ошибок
sudo journalctl -u pixlive --since "5 min ago" -p err
```

### Нет прав на перезагрузку в update.sh

```bash
# Либо запусти с sudo
sudo bash update.sh

# Либо добавь свой пользователь в sudoers
sudo visudo
# Добавь: username ALL=(ALL) NOPASSWD: /bin/systemctl
```

---

## 📝 Техподдержка

Если возникли проблемы:

1. Проверь логи: `sudo journalctl -u pixlive -f`
2. Запусти тесты: `cd /opt/PixLiveDiscordBot && source venv/bin/activate && python test_integration.py`
3. Проверь конфиг: `python check_config.py`
4. Создай issue на GitHub

---

**Версия:** 1.0  
**Последнее обновление:** 16 декабря 2025  
**Лицензия:** MIT
