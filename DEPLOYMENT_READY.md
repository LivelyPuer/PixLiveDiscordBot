# ✅ Развертывание PixLive Discord Bot - Готово!

## 📦 Что было добавлено

Проект подготовлен к деплою на сервер с полной автоматизацией. Все компоненты готовы к production использованию.

### 🎯 Основные скрипты

#### 1. **`install.sh`** - Одна команда для полного деплоя ⭐

```bash
# Развертывание с GitHub
curl -fsSL https://raw.githubusercontent.com/LivelyPuer/PixLiveDiscordBot/main/install.sh | sudo bash

# Или локально
sudo bash install.sh
```

**Что делает:**
- ✅ Определяет ОС и устанавливает зависимости
- ✅ Клонирует репо в `/opt/PixLiveDiscordBot`
- ✅ Создает Python venv и устанавливает dependencies
- ✅ **Создает systemd сервис для автозапуска**
- ✅ **Включает автоматический перезапуск при падении**
- ✅ Запускает бота

#### 2. **`deploy.sh`** - Полное управление

```bash
sudo bash /opt/PixLiveDiscordBot/deploy.sh
```

Интерактивное меню или команды:
```bash
install   # Полная установка
update    # Обновление кода
start     # Запустить
stop      # Остановить
restart   # Перезагрузить
status    # Статус
logs      # Логи
```

#### 3. **`update.sh`** - Быстрое обновление

```bash
sudo bash /opt/PixLiveDiscordBot/update.sh
```

Обновляет код и перезагружает сервис

#### 4. **`run.sh`** - Локальный запуск

```bash
bash run.sh
```

Для разработки и локального тестирования (без root)

### 📚 Документация

#### 1. **`QUICKSTART.md`** - Быстрый старт
Элементарный старт - одна команда и готово

#### 2. **`DEPLOYMENT.md`** - Полная документация
Полное руководство по развертыванию, управлению, обновлению и решению проблем

#### 3. **`SCRIPTS_FULL.md`** - Документация скриптов
Подробное описание каждого скрипта и когда его использовать

#### 4. **`pixlive.service`** - Systemd файл
Шаблон для systemd сервиса (используется скриптами)

### 🔄 Автозапуск и автопереза

**Включен по умолчанию**:

```bash
# Бот запустится при перезагрузке сервера
# Если упадет - автоматически перезагрузится через 10 секунд
```

Управление:
```bash
sudo systemctl status pixlive     # Статус
sudo systemctl start pixlive      # Запустить
sudo systemctl stop pixlive       # Остановить
sudo systemctl restart pixlive    # Перезагрузить
sudo systemctl is-enabled pixlive # Проверить автозапуск
```

### 📋 Логи

```bash
# Живые логи
sudo journalctl -u pixlive -f

# Последние 50 строк
sudo journalctl -u pixlive -n 50

# Только ошибки
sudo journalctl -u pixlive -p err
```

## 🚀 Как начать

### На сервере (Linux)

```bash
# 1. Одна команда для всего
curl -fsSL https://raw.githubusercontent.com/LivelyPuer/PixLiveDiscordBot/main/install.sh | sudo bash

# 2. Отредактировать конфигурацию
sudo nano /opt/PixLiveDiscordBot/.env

# 3. Готово! Бот работает и будет перезагружаться автоматически
```

### Локально (для разработки)

```bash
# 1. Клонировать репо
git clone https://github.com/LivelyPuer/PixLiveDiscordBot.git
cd PixLiveDiscordBot

# 2. Запустить
bash run.sh

# 3. Нажать Ctrl+C для остановки
```

## 📊 Типичные операции

### Проверить что бот работает

```bash
sudo systemctl status pixlive
sudo journalctl -u pixlive -n 20
```

### Обновить код

```bash
sudo bash /opt/PixLiveDiscordBot/update.sh
```

### Редактировать конфигурацию

```bash
sudo nano /opt/PixLiveDiscordBot/.env
sudo systemctl restart pixlive
```

### Остановить бота

```bash
sudo systemctl stop pixlive
```

### Перезапустить бота

```bash
sudo systemctl restart pixlive
```

## ✨ Ключевые особенности

| Особенность | Статус |
|-----------|--------|
| Одна команда для деплоя | ✅ |
| Автозапуск при старте сервера | ✅ |
| Автоматическая перезагрузка при падении | ✅ |
| Управление через systemd | ✅ |
| Логирование в journal | ✅ |
| Обновление с GitHub одной командой | ✅ |
| Поддержка Ubuntu/Debian/CentOS/Alpine | ✅ |
| Интерактивное меню управления | ✅ |
| Полная документация | ✅ |

## 📁 Структура файлов на сервере

```
/opt/PixLiveDiscordBot/
├── install.sh              # Скрипт установки
├── deploy.sh               # Полное управление
├── update.sh               # Обновление
├── run.sh                  # Локальный запуск
├── pixlive.service         # Systemd файл
├── main.py                 # Точка входа
├── requirements.txt        # Python зависимости
├── .env                    # Конфигурация (ВАЖНО)
├── README.md              # Основная документация
├── QUICKSTART.md          # Быстрый старт
├── DEPLOYMENT.md          # Полная документация
├── SCRIPTS_FULL.md        # Документация скриптов
├── venv/                  # Python окружение
├── bot/                   # Код бота
├── services/              # Сервисы
├── data/                  # Данные и состояние
└── tests/                 # Тесты
```

## 🔐 Безопасность

1. **`.env` файл** - хранит все секреты (токены, пароли)
   - Создается с правами 600 (только для пользователя)
   - Никогда не коммитить в git
   - Хранить в безопасности

2. **Пользователь `pixlive`** - бот работает под отдельным пользователем
   - Не имеет прав root
   - Ограничен доступом к системе

3. **Systemd сервис** - настроен с ограничениями безопасности
   - NoNewPrivileges (не может повысить привилегии)
   - PrivateTmp (отдельная временная папка)
   - ProtectSystem (только для чтения основные папки)

## 🆘 Помощь

### Если что-то не работает

1. **Проверь логи**
   ```bash
   sudo journalctl -u pixlive -f
   ```

2. **Проверь конфигурацию**
   ```bash
   cat /opt/PixLiveDiscordBot/.env
   ```

3. **Переустанови если нужно**
   ```bash
   sudo bash /opt/PixLiveDiscordBot/deploy.sh install
   ```

### Документация

- 📖 [README.md](README.md) - Основная документация
- 🚀 [QUICKSTART.md](QUICKSTART.md) - Быстрый старт
- 📋 [DEPLOYMENT.md](DEPLOYMENT.md) - Развертывание
- 🔧 [SCRIPTS_FULL.md](SCRIPTS_FULL.md) - Скрипты
- 🎯 [SETUP.md](SETUP.md) - Получение токенов

## ✅ Checklist перед production

- [ ] Все требуемые токены добавлены в `.env`
- [ ] Протестировано локально (`bash run.sh`)
- [ ] Запущен `install.sh` на целевом сервере
- [ ] Отредактирован `.env` на сервере
- [ ] Проверен статус: `sudo systemctl status pixlive`
- [ ] Проверены логи: `sudo journalctl -u pixlive -n 50`
- [ ] Бот успешно постит сообщения в Discord

---

## 🎉 Готово к использованию!

Проект полностью подготовлен к production развертыванию. 

**Для развертывания на сервер используй одну команду:**

```bash
curl -fsSL https://raw.githubusercontent.com/LivelyPuer/PixLiveDiscordBot/main/install.sh | sudo bash
```

**После установки:**
- Отредактируй `.env`: `sudo nano /opt/PixLiveDiscordBot/.env`
- Проверь статус: `sudo systemctl status pixlive`
- Смотри логи: `sudo journalctl -u pixlive -f`

Бот будет работать автоматически, перезагружаться при падении и запускаться при старте сервера.

Happy deploying! 🚀
